[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "autobuilding"
version = "1.0.0"
description = "Auto-building tool for python modules."
authors = [{name="wirnty", email="skedovichusjdj@gmail.com"}]
